const c=game,ctx=c.getContext('2d');let p={x:220,y:500,msg:'READY'};let enemies=[{x:850,h:100},{x:1050,h:100}];let score=0,timeLeft=30;setInterval(()=>{if(timeLeft>0){timeLeft--;time.textContent=timeLeft;}},1000);function move(v){p.x=Math.max(80,Math.min(1180,p.x+v));}function jump(){p.y=420;setTimeout(()=>p.y=500,250);}function crouch(){p.y=540;setTimeout(()=>p.y=500,250);}function attack(d,m){p.msg=m;enemies.forEach(e=>{if(Math.abs(e.x-p.x)<240){e.h-=d;if(e.h<=0){score+=100;scoreEl();}}});enemies=enemies.filter(e=>e.h>0);}function scoreEl(){document.getElementById('score').textContent=score;}function player(){ctx.fillStyle='#8d5524';ctx.beginPath();ctx.arc(p.x,p.y-60,36,0,6.28);ctx.fill();ctx.fillStyle='black';for(let i=0;i<18;i++)ctx.fillRect(p.x-30,p.y-65+i*5,60,4);ctx.fillStyle='white';ctx.fillRect(p.x-30,p.y,60,100);ctx.fillStyle='gold';ctx.fillRect(p.x-14,p.y+15,28,6);}function draw(){ctx.clearRect(0,0,1280,720);ctx.fillStyle='#16a34a';ctx.fillRect(0,620,1280,100);ctx.fillStyle='white';ctx.font='28px Arial';ctx.fillText('🌴 MIAMI OCEAN DRIVE',880,50);ctx.fillText('Richway Designs',1000,700);ctx.fillText(p.msg,40,50);player();enemies.forEach(e=>{if(e.x>p.x+120)e.x-=0.8;ctx.fillStyle='#444';ctx.fillRect(e.x-24,500,48,100);ctx.fillStyle='#b7791f';ctx.beginPath();ctx.arc(e.x,455,28,0,6.28);ctx.fill();ctx.fillStyle='red';ctx.fillRect(e.x-40,420,e.h*.7,10);});if(!enemies.length){ctx.font='70px Arial';ctx.fillText('🏆 YALL KNOW',400,320);}requestAnimationFrame(draw);}draw();